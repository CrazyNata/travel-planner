# Copy this file to /etc/ramingo/payment-reminders.env on the VPS.
# Keep the real secret out of git and make the file readable only by root.
SUPABASE_URL=https://hxcavgtlucyoqudbrgse.supabase.co
PAYMENT_REMINDERS_SECRET=replace-with-a-long-random-secret
